<html>
  <head>
    <!-- set styling file -->
    <link rel="stylesheet" href="/styles.css">
    <title>Category - <%= category_name %></title>
  </head>
  <body>
    <a href="/"><button>Back to Home</button></a>

    <h1><%= category_name %></h1>

    <a href="/category/<%= category_name %>/categoryItemForm"><button>Add an item to <%= category_name %></button></a>

    <% for (let i = 0; i < items.length; i++) { 
      curItem = items[i]; %>
      <p>
        <form method="POST" action="/deleteItem/<%= category_name %>/<%= curItem.item_name %>">
          <button class="delete-btn">X</button>
        </form>

        <a href="/category/<%= category_name %>/item/<%= curItem.item_name %>" ><button><%= curItem.item_name %></button></a>
        <%= `Stock: ${curItem.stock}` %>
        <%= `Description: ${curItem.description}` %>
      </p>
    <% } %>
    
    <%- include('./partials/footer') %>
  </body>
</html>